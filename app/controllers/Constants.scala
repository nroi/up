package controllers

object Constants {
  val hostname = "up.helios.click"
  val directory = "f"
  val hashDirectory = "hash"
  val retrievalUrl = s"https://$hostname/$directory"
  val MinDigestLength = 2
}
